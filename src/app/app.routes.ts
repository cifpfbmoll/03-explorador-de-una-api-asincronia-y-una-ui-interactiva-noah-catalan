import { Routes } from '@angular/router';

/**
 * Rutas de la aplicación
 * Por ahora solo tenemos la ruta principal, pero aquí se pueden agregar más rutas en el futuro
 */
export const routes: Routes = [];
